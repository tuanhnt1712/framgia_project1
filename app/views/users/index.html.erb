<% provide :title, t(".users") %>
<h1><%= t ".all_users" %></h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<div class="page-center">
  <%= will_paginate %>
</div>
